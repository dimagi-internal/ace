# Plan A — Task 1 discovery findings

**Date:** 2026-07-20

## Denovo atom set — CONFIRMED present (from the live MCP tool registry)

All present as `mcp__connect_labs__*`: `synthetic_generate_from_manifest`, `synthetic_create_labs_only`, `workflow_create`, `workflow_update_definition`, `workflow_update_render_code`, `workflow_patch_render_code`, `pipeline_update_schema`, `workflow_create_run`, `workflow_save_snapshot`, `workflow_get`, `synthetic_env_ensure`, `synthetic_env_list`, `synthetic_env_get`.

## Canopy handoff — LOCKED

- **Checkout:** `/Users/jjackson/emdash-projects/canopy` (there are also worktrees under `~/emdash/worktrees/canopy*`; use the emdash-projects one as the primary — it is where `uv run` built the package cleanly).
- **`uv`:** available at `~/.local/bin/uv`.
- **Validate CLI:** `cd /Users/jjackson/emdash-projects/canopy && uv run python -m scripts.ddd.validate <kind> <path>` — exit `0` = valid, `1` = invalid (prints problems), `2` = usage error. Confirm the exact `<kind>` tokens (`why_brief`, `unified_spec`) from `validate()`'s dispatch in `scripts/ddd/validate.py` on first live use.
- **Published JSON Schemas:** `scripts/narrative/schema/json/` — `UnifiedSpec.json`, `WhyBrief.json`, `Feature.json`, `RunState.json`, `Verdict.json`, `Decision.json`, `ReviewRequest.json`. Regenerated by `scripts/ddd/regen_json_schemas.py` / the `dump_json_schemas` path in `validate.py`.
- **Single render+judge:** `/canopy:ddd-run` (args `run_id`, `unified_spec`, `why_brief`). Full loop: `canopy:ddd` agent. Video bridge: `/canopy:ddd-ace-render`.
- **Scene surface base_url:** `https://labs.connect.dimagi.com`.

## Run-state contract — CORRECTED (this changes the plan's Task 2 draft)

From `lib/run-state-validator.ts`:
- **Allowed phase `status`:** `pending | in_progress | done | error | blocked | skipped`. **No `not-applicable`; it is `in_progress` with an underscore.** → not-run phases use `status: skipped`; the live phase uses `status: in_progress`.
- **`steps` is a MAPPING** (`{}`), not an array. `verdict` / `summary_artifact` must be **omitted** when absent (a `null` fails the string check).
- `classifyPhaseWriteBack(rs, phase)` returns `in_progress` for the live phase (not `malformed`).

## Realized-map contract (from prior exploration; live capture deferred to joint test)

`synthetic_env_ensure` returns a flat `${var}` map whose key handoff value is:
- `par_url` = `/labs/workflow/<def_id>/run/?run_id=<run_id>&opportunity_id=<opp_id>` (the polished dashboard deep-link; the bare workflow URL renders the run picker — `docs/learnings/2026-06-13-labs-workflow-run-deeplink.md`).
- plus drill URLs (e.g. `audit_good_url`, `task_good_url`, `chc_good_url`, `wk4_url`).

**Live capture (done 2026-07-20, `synthetic_env_ensure env=program-admin-report`):**
```
par_url            /labs/workflow/65/run/?run_id=4539&opportunity_id=10000   (program_admin_report — run-shaped)
wk4_url            /labs/workflow/3149/run/?run_id=4877&opportunity_id=10000  (chc_nutrition_analysis current week)
chc_good_url       /labs/workflow/3149/run/?run_id=4503&opportunity_id=10000  (chc resolved week)
audit_good_url     /audit/4525/?opportunity_id=10000     task_good_url  /tasks/4533/edit/?opportunity_id=10000
audit_incomplete_url /audit/4528/?opportunity_id=10001   task_incomplete_url /tasks/4536/edit/?opportunity_id=10001
opps: 10000 Northern / 10001 Southern   flagged FLWs: hawa_n (good), ola_s (incomplete), jumoke_n (manager)
```
So env-ensure returns FLAT `${var}` keys (par_url, wk4_url, *_good_url, *_incomplete_url) — matches the demo-data-setup realized-map contract. Drills `/audit/<id>/` + `/tasks/<id>/edit/` are action-shaped (no run_id), consistent with the run|action shape split.

**Dry-run finding — demo-data-setup needs a second generation path.** Composite multi-opp rollups (`program_admin_report`) are populated by the ENV-ENSURE ensurer chain (weekly_runs→run_audits→rollup), NOT by `generate_from_manifest` + `workflow_create`. So `demo-data-setup` must support a `realize-env` mode (`synthetic_env_ensure`) for composite dashboards, alongside the from-manifest mode for FLW-level ones. The per-child `sam_followup` recovery dashboard is action-shaped and is NOT in the PAR env — it's the genuine net-new build for the RUTF child-recovery narrative.

## Bottom line

**Denovo path needs zero labs changes: CONFIRMED.** All atoms exist; the dashboard is authored dynamically via the `workflow_*` atoms (as Phase 7 already does); canopy validate + `/canopy:ddd-run` are reachable via `uv` from the local canopy checkout. Proceed with Plan A.
