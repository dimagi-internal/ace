# Advance to the next form question on FormEntryActivity by tapping the
# `nav_btn_next` ImageButton (right-edge `>` arrow).
#
# Atlas §7. The button has NO text — it's an ImageButton with only a
# resource-id, so text-match selectors (the prior recipe used
# `text: "Next"`) tap nothing or tap something else entirely. Use the
# resource-id-based `form-nav-next` selector.
#
# On the LAST question of a form, the same tap auto-finalizes + submits
# the form locally and returns to StandardHomeActivity. For Learn-app
# forms there is no explicit Submit button — the last `nav_btn_next` IS
# the finalize action. For Deliver-app forms, behavior is TBD until the
# atlas walks Deliver.
#
# Parameters — REQUIRED AT EVERY CALL SITE:
#   ${SCREENSHOT_NAME} — name for the screenshot of the screen being LEFT,
#                        captured BEFORE the advance (dimagi-internal/ace#1291).
#                        Composer fills with e.g. "form-q3-photo-rendered"
#                        or "form-q11-yes-conditional-q12-shown".
#
# Every `runFlow` of this palette MUST bind ${SCREENSHOT_NAME} in its own
# `env:` block — and because form-advance is called once per form screen,
# each call site needs its OWN name (a shared one overwrites earlier frames).
# A bare `runFlow: { file: form-advance.yaml }` emitted a literal
# `undefined.png` on bednet-spot-check/20260728-2222 (dimagi-internal/ace#1033,
# the recurrence of #852 through this file).
#
# There is deliberately NO `env:` fallback block here. A Maestro flow's own
# top-level `env:` does NOT act as a default — it OVERRIDES caller-passed
# `runFlow: env:` (Maestro 2.5.1: the subflow's DefineVariablesCommand is
# executed AFTER the caller's, and `putEnv` assigns unconditionally). Adding
# a default here would silently defeat per-call-site naming, which is exactly
# what the #852 fix did to form-submit.yaml. Unbound call sites are caught at
# authoring time instead, by the `runFlow-unbound-screenshot-name` rule in
# `mcp/mobile/recipe-lint.ts` (run by `mobile_validate_recipe`).
#
# Failure modes:
#   - Required-input violation: the `nav_btn_next` tap fires but the
#     surface stays put and the `warning_root` banner appears with text
#     "Sorry, this response is required!". Caller should `assertNotVisible:
#     id: form-required-warning` after advancing on questions that DO
#     require input — confirms the answer registered.
#   - Form auto-finalize: tapping next on the last question returns to
#     StandardHomeActivity (no animation back to a "next question").
#     Since ace#1291 the screenshot is taken BEFORE the tap, so it shows
#     the last QUESTION rather than the home tiles. Callers that want the
#     post-finalize surface must assert on the home tile directly
#     (`learn-home-start-tile` / `deliver-home-job-card`) — a screenshot
#     named for the question it left is not a "form complete" signal.
#
# SCREENSHOT ORDER — BEFORE the tap, deliberately (dimagi-internal/ace#1291).
# This recipe used to tap first and capture second, so every frame was saved
# under the name of the screen it had just LEFT while showing the one it
# advanced TO. Verified visually on bednet-check-2-visit/20260814-0357:
# `journey-learn-m1-intro.png` held the b1 question, and
# `journey-learn-m2-t2-payment.png` held t3 — a systematic one-screen offset
# across all 8 teaching screens and every `*-answered` step. The manifest is
# the input contract for `training-flw-guide` and `training-deck-generate`,
# both of which caption by step name, so a deck built from it asserted the
# wrong screen for every advance-derived frame — the ace#866 class, and
# invisible unless someone opens the PNGs. Capturing first also means the
# FIRST screen of a form is finally captured under its own name.
appId: org.commcare.dalvik
---
- takeScreenshot: "${SCREENSHOT_NAME}"
- tapOn:
    ${SELECTOR:form-nav-next}
- waitForAnimationToEnd:
    timeout: 3000
