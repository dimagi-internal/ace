# Drive a claimed opportunity from the post-claim handoff state through
# the StandardHomeActivity Start tile into the Learn-app's MenuActivity
# suite root.
#
# Pre-state: post-`btn_start` transition completed; device is on
# StandardHomeActivity (the CommCare app home with 4 tiles: Start, View
# Job Status, Sync with Server, Log out of CommCare). The recipe author
# enters this state when `connect-claim-opp.yaml` has finished, fired
# `btn_start`, and the Learn CCZ download completed (`Downloading
# Learn App` progress screen auto-advanced).
#
# Post-state: MenuActivity showing the Learn-app's suite root (1+ rows,
# each a tappable LinearLayout containing `row_img` + `row_txt`, inside
# the app's menu container).
#
# MENU ANCHOR RULE (dimagi-internal/ace#1127): the landing wait uses
# `${SELECTOR:learn-suite-menu}`, which is DISPLAY-MODE-AGNOSTIC — it
# accepts the LIST container (`screen_suite_menu_list`) OR the GRID
# container (`grid_menu_grid`). CommCare renders the SAME rows in either;
# only the container id changes with the app's menu display setting. This
# recipe hard-halted every ACE Phase 6 the moment #1082/PR #1100 made
# Phase 3 apply grid display app-wide, because the anchor resolved to the
# list container alone. Never narrow it back to one container.
#
# The selector map is ground truth; the atlas below is narrative only.
# Atlas reference: docs/mobile-atlas/connect-2.63.2.md § 5 — the Learn
# StandardHomeActivity + suite/form-list MenuActivity are carried over there
# from the 2.62.0 walk and NOT re-walked on 2.63.2; § 5 states exactly what
# the 2.63.0 -> 2.63.2 drift check did and did not establish about them.
appId: org.commcare.dalvik
---
# ── CommCare version gate (ace#1998) ─────────────────────────────
# If the released CCZ requires a newer CommCare than the pinned APK,
# CommCare interposes an upgrade gate instead of opening the app, and
# every wait below would instead expire generically (30s here) with no
# clue which system refused. Recognise it FIRST and fail with its own
# name. Root cause + remedy: ace#1997 (repin ACE_CONNECT_APK_VERSION).
#
# Recognise-only, by design — this deliberately does NOT tap
# `commcare-version-update-later`. The button's existence is recorded
# device evidence; what dismissing it PERMITS has never been observed,
# and proceeding on a runtime the CCZ declares unsupported would mint
# false-green Phase 6 screenshots and QA verdicts. See
# connect-claim-opp.yaml § the version-gate branch for the full argument.
- runFlow:
    when:
      visible:
        id: "${SELECTOR:commcare-version-prompt-title}"
    commands:
      - takeScreenshot: "learn-launch-BLOCKED-commcare-version-gate-issue1997"
      - assertNotVisible:
          id: "${SELECTOR:commcare-version-prompt-title}"

# StandardHomeActivity gives us 4 tiles. We want the green Start tile.
- extendedWaitUntil:
    visible:
      ${SELECTOR:learn-home-start-tile}
    timeout: 30000
- takeScreenshot: "learn-launch-home-tiles"

# Detect the "Failed to start learning" failure mode early. Connect
# surfaces this banner within ~2 seconds when its CCHQ Learn-app fetch
# fails — typically (a) the API key user lacks app-edit access on the
# released build, (b) the released CCZ format mismatches what Connect
# expects, or (c) Connect cached stale Learn-app metadata from a
# prior run. Without this assertion the recipe would time out 30s
# later on `extendedWaitUntil(modules-list)` with no diagnostic clue.
# Tracking: jjackson/ace#115 finding 1.
- assertNotVisible:
    text: "Failed to start learning"

# Tap the Start tile. Maestro walks up the hierarchy from the
# `card_text` TextView to the clickable `card` RelativeLayout
# (the TextView itself is not clickable; only the parent card is).
- tapOn:
    ${SELECTOR:learn-home-start-tile}

# Landing surface: MenuActivity (suite root). Anchor is a single row
# containing the app's display name (e.g. "Turmeric Market Survey — Learn").
# The exact text varies per opp; using the structural anchor (the menu
# container, list OR grid — see MENU ANCHOR RULE above) keeps the recipe
# both opp-agnostic and display-mode-agnostic.
- extendedWaitUntil:
    visible:
      id: "${SELECTOR:learn-suite-menu}"
    timeout: 15000
- takeScreenshot: "learn-launch-suite-root"
