# Issue triage — dimagi-internal/ace — 2026-07-24

Triaged 29 of 29 open issues (no truncation) against local working tree at `d5c1f198` (current main). One subagent per issue, read-only.

## Summary

- **close**: 2 (#895, #860 — both duplicates of other open issues)
- **investigate**: 1 (#811 — needs one on-device A/B run; draft PR #850 already holds the fix)
- **implement**: 26

## Verdicts

| # | Disposition | Conf | Effort | Title | Evidence / why | Action taken |
|---|-------------|------|--------|-------|----------------|--------------|
| 895 | close | high | — | Phase 6: FLW invite→OpportunityAccess propagation lag + claim stale-cache | Duplicate: stale-claim half owned by #811 (root-caused: `action_sync` is the wrong refresh affordance, `connect-claim-opp.yaml:95-100`); invite-linkage half owned by #824/#855. Scroll-softening ask would regress the #647 calibration (`connect-claim-opp.yaml:186-215`). | closed (dup; evidence cross-linked to #811/#855) |
| 860 | close | high | — | recipe-sanity-probe: form-advance false-positives on label chains (re-file) | Duplicate of #858 — same file/function/root cause/repro (`recipe-sanity-probe.ts:443`, run 20260702-1456, same Nova app IDs). #858 is older + superset. Port #860's WARN-fallback suggestion into #858 as a comment. | closed (dup of #858; WARN-fallback ported) |
| 811 | investigate | med | S | connect-claim-opp: action_sync does not refresh Connect invite list | Code contradiction: in-file #628 rationale (`connect-claim-opp.yaml:77-94`) claims `action_sync` IS the fix, live-calibrated; #811 claims the opposite, live-calibrated. Draft PR #850 holds the swipe-refresh fix. Only an on-device back-to-back run (fresh Phase-4 invite, both sequences, ui-dumps) can adjudicate. If swipe wins: ready #850, mirror into `connect-resume-opp.yaml:112`, add invariant to `static-recipe-invariants.test.ts`. | A/B attempted live 2026-07-25 — blocked by #938 crash; protocol posted on issue |
| 917 | implement | high | S | registries-coverage: 2 tests failing on main | `artifact-manifest.ts:677` registers `app-hq-settings` as producer; no row in `skills/_qa-decisions.md` / `_eval-decisions.md`; not exempted (`registries-coverage.test.ts:43-61`). Fix = two markdown rows. CI (`clean-install.yml:27-37`) doesn't run vitest — secondary claim confirmed. | closed — already fixed on main by PR #934 (landed mid-triage) |
| 931 | implement | high | S | scenarios-and-acceptance QA description stale vs checks.ts | `agents/scenarios-and-acceptance.md:88` says 5 categories/≥15%; `pdd-to-test-prompts-qa/checks.ts:25-36` says 7/≥20%. Two more stale copies found: `skills/_qa-decisions.md:66`, `skills/pdd-to-test-prompts/SKILL.md:134`. | fixed — PR #937 merged |
| 929 | implement | high | S | Step 5b auto-migrate would move comms-logs into inputs/ | `ace-orchestrator.md:681-691` skip-list is literally just `opp.yaml`; `lib/doctor-drive-layout.ts:39` whitelist also lacks comms-log entry. Fix both + test. | fixed — PR #939 merged |
| 876 | implement | high | S | agent-turn-review: re-evaluate re-sent/linked artifacts | Neither ACE stub (`skills/agent-turn-review/SKILL.md:12,89`) nor canopy 0.2.347 checklist has an artifact re-evaluation item. ACE half is one checklist item; canopy half is an upstream PR. | fixed — PR #940 merged (ACE half; canopy half upstream) |
| 888 | implement | high | S | doctor canopy_email_engine probe: flag version skew | `bin/ace-doctor:1852-1858` checks presence only. `canopy email preflight` already runs the exact staleness check with non-zero exit + FIX line — probe can shell out to it. | fixed — PR #943 merged |
| 853 | implement | high | S | resolveManifest passes bare Drive fileIds to Slides createImage | `lib/training-deck-spec.ts:462-468` `@alias` branch only handles `drive:` prefix; `normalizeDriveImageUrl:400` passes bare ids through. One-line branch mirroring #724 fix at :448 + test. | fixed — PR #941 merged |
| 852 | implement | high | S | form-submit palette: SCREENSHOT_NAME_PRE/POST_SUBMIT unbound | `form-submit.yaml:36,77` placeholders with no defaults; resolver passes env through (`recipe-resolver.ts:280`); no composition rule in `app-test-cases/SKILL.md:304`; no lint row. | fixed — PR #942 merged |
| 870 | implement | high | S | Selector map: add opp-list-view-info (btn_view_info) | Absent from both maps; resource-id corroborated by `docs/mobile-calibration/connect-2.63.0-2026-06-01.md:87` (no device time needed for the row). Optional palette adoption also unimplemented. | fixed — PR #944 merged (selector row; palette adoption held for live validation) |
| 861 | implement | high | S | Selector map: geopoint capture-button row (RECORD LOCATION) | `connect-2.63.0.yaml:331-356` still comment-only NOTE; `app-test-cases/SKILL.md:605-627` still says `recipe: deferred`. `mobile_set_location` exists (AVD-only — add caveat). | fixed — PR #945 merged |
| 869 | implement | high | S | Static palettes capture up to 3 byte-identical Deliver-home frames | All three `takeScreenshot` steps still present (`deliver-launch.yaml:89,203`, `deliver-form-walk.yaml:25`); `connect-resume-opp.yaml:116,135` pair undocumented. Only downstream `duplicate_of` handling (#866) shipped. | open (implement; palette shot deletions need a live-validated pass) |
| 906 | implement | high | S | share-run-access: ocs_add_team_member atom | No team capability in `mcp/ocs/capability-map.ts`; whole verified mechanic already in `scripts/grant-review-access.ts:923-1170` (invite POST, group reconciliation, read-back) — lift-and-shift into OCS backend + tool registration + SKILL.md rewrite. | fixed — PR #947 (auto-merge armed) |
| 892 | implement | high | S | commcare-setup reports done/pass while deferring app-test-cases | Verdict gate (`agents/commcare-setup.md:541`) only fires on deferred `-eval` of `has_judge: true` skills; `app-test-cases` is `has_judge: false`. `3-commcare/recipes/journey-learn.yaml` has no manifest entry despite being Phase 6's hard floor (`qa-and-training.md:193`). | fixed — PR #946 merged |
| 933 | implement | high | M | Consume inputs/decision-overrides.yaml when seeding runs | Zero references repo-wide; `lib/decisions-sync.ts:41-97` has the exact merge semantics to reuse; needs schema + application point + manifest registration + evidence-pack exclusion. | closed — already implemented on main by PR #935 (landed mid-triage) |
| 905 | implement | high | M | share-run-access: commcare_invite_web_user atom | Atom absent from `mcp/connect-server.ts` registry; `SKILL.md:57,139-142` hardcodes manual workaround citing #905. `setUserFields` (`backends/commcare.ts:1588`) establishes the CSRF form-POST pattern to mirror; needs live HQ form-field discovery. | open (implement M; needs live HQ form discovery) |
| 891 | implement | high | M | ocs_publish_chatbot_version: API read-back fallback | Badge-scrape throw still at `playwright.ts:955-969`; REST `getChatbot` reads `version_number` but is keyed by `public_id` — needs id bridge + composite-level wrapper + rewrite of #823 regression test. | open (implement M) |
| 890 | implement | high | M | Sourcing login-gated instruments (Box JS-viewer downloads) | No skill/integration doc/helper exists; `mcp/lib/playwright-session.ts` does auth only, no download capture; playwright already a dep. Secondary turn-review ask already landed. | open (implement M) |
| 880 | implement | high | M | /ace:setup env changes need restart; doctor env-freshness | All three fixes absent: no restart banner in `bin/ace-setup`, no env-freshness probe in `bin/ace-doctor` (only version at :431), CLAUDE.md §restart covers code not env. Mechanism confirmed at `connect-server.ts:18,61`. | open (implement M) |
| 873 | implement | high | M | Common pool: PersonalID completion-half screenshots | All 5 aliases still fixture/placeholder (`common-screenshot-capture/SKILL.md:54-58`); fixtures dir empty; `platform-setup.yaml:37-40` still references them; no non-demo capture identity. Needs an option decision (capture on device = M; drop aliases = S). | open (needs option decision) |
| 893 | implement | high | M | deliver-launch: viewJobCard differentiator false on single-module builds | `deliver-launch.yaml:84,154,209` still key on `viewJobCard`; selector + atlas docs still assert disproven "absent in Learn". Needs new live-verified selectors (`cvConnectMessage` / View Job Status); live validation gated on #897 Deliver-eligibility. | open; consolidation comment posted (differentiator cluster w/ #863/#796) |
| 863 | implement | high | M | connect-claim-opp: already-Learn-complete misses installed-Deliver landing | #570 branch keyed only on `deliver-download-button` (`connect-claim-opp.yaml:464-469`); fresh-Learn branch's `nsv_home_screen` wait passes on Deliver home. Resolve differentiator against #893 (prefer action-bar title over viewJobCard). | open; consolidation comment posted |
| 862 | implement | high | M | app-test-cases: Nova groups render as ONE field-list screen | No group carve-out in `SKILL.md:569-614`; no `group-field-list-per-question-walk` class in sanity probe; `NovaAppSlice` carries no field kinds. | open (implement M; shares NovaAppSlice work with #858) |
| 858 | implement | high | M | recipe-sanity-probe: label-blind form-advance chain false positives | `recipe-sanity-probe.ts:443` fires unconditionally on ≥2 chains; `NovaAppSlice:79` has no field kinds. Keep open as canonical; fold #860's WARN-fallback. | open (canonical; #860 folded in) |
| 856 | implement | high | M | qa-and-training: hollow training deck ships as proceed-with-warn | Degraded path codified verbatim (`training-deck-generate/SKILL.md:85,96`); no visual-coverage gate in any of the 4 skill docs; no `visual_coverage` field in `lib/verdict-schema.ts`. | open (implement M) |
| 855 | implement | high | M | Phase 6 preflight: verify a LINKED FLW invite | All 3 asks unimplemented; `connect-opp-setup/SKILL.md:570-596` explicitly defers to now-closed #799. Deterministic remediation known (register → delete → re-invite → poll). Same atom as #824 — implement together. | open; consolidation comment posted (one impl with #824) |
| 824 | implement | high | L | FLW invite lands unlinked; 202-queued read as success (class fix) | All 3 preventers absent (Phase-4 read-back, Phase-6 re-invite+verify, `connect_list_flw_invites` atom). Consolidate: #824 = class-fix of record; fold #855's remediation + (closed) #895's poll into it. | open; kept as class-fix of record, consolidated scope posted |
| 796 | implement | high | M | Phase 6 retry non-idempotent on already-walked run | 2 of 3 fixes outstanding; NEW: `connect_get_learn_progress` (#897) now enables the cheap pre-AVD `learn_complete` probe headlessly; recipe branch still needs a live ui-dump from a Deliver-complete opp. Orchestrator half (fix 3) already resolved. | open; scope-shrink comment posted (pre-AVD probe now implementable) |

## Cross-issue structure

- **FLW-invite cluster:** #824 (class fix, L) + #855 (canonical preflight spec, M) + #895 (close as dup). One implementation: `connect_list_flw_invites` atom + Phase-4 WARN read-back + Phase-6 preflight verify.
- **Learn/Deliver-home differentiator cluster:** #893 + #863 + #796 all need a reliable "which home am I on" signal; #893 shows `viewJobCard` is unreliable — resolve with the suite action-bar title, one live calibration session covers all three.
- **Sanity-probe structure cluster:** #858 (keep) + #860 (close as dup) + #862 all want `NovaAppSlice` extended with field kinds — one probe change serves both classes.

## Post-gate outcome (2026-07-25)

- 8 PRs merged: #937 (931), #939 (929), #940 (876), #941 (853), #942 (852), #943 (888), #944 (870), #945 (861), #946 (892), #947 (906) — 10 total.
- 4 issues closed without PRs: #895 + #860 (dups), #917 + #933 (fixed on main mid-triage by PRs #934/#935 from a parallel session).
- 2 new issues filed: #936 (add-org-member tests — closed same hour, already fixed by #934) and #938 (CommCare 2.63.0 jobs-list NPE crash loop — HARD-BLOCKS all local Phase 6 / mobile work, incl. the #811 A/B).
- #811 A/B: attempted live; AVD healed through location-services + registration blockers, then hit the #938 APK crash. Full adjudication protocol posted on #811; draft PR #850 stays draft.
- Remaining open from this triage: 11 implement issues (#824/#855 cluster, #893/#863/#796 cluster, #858/#862 probe pair, #856, #891, #890, #880, #873, #905, #869).
