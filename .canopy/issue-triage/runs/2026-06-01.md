# Issue Triage — jjackson/ace — 2026-06-01

Triaged **5 of 5** open issues (no truncation). Code evaluated: local working tree
(`emdash/worktrees/ace/emdash/issues-sc1cx`, branch `emdash/issues-sc1cx`, tip `bec9328`).

| #   | Disposition | Conf | Effort | Title | Evidence / why | Action taken |
|-----|-------------|------|--------|-------|----------------|--------------|
| 660 | close | high | — | Document deliver_unit marker mechanism (module_type, not connect:{}) | `skills/pdd-to-deliver-app/SKILL.md:51-72` documents `module_type="connect.deliver_unit"` + sibling `entity_id`/`entity_name` and warns against nested `connect:{}`; `playbook/integrations/nova-integration.md:290-301` corrects the old `update_form` claim. Both shipped in commit `581cf13`. | **Closed** with reasoned comment citing file:line evidence. |
| 618 | implement | high | M | Phase 6 inter-leg nav gap: journey-learn lands in CommCare home, connect-resume-opp asserts jobs-list | `connect-resume-opp.yaml:46-48` still hard-asserts `${SELECTOR:home-jobs-list}` as step 1 with no preceding back-out-of-CommCare-home branch; `skills/app-test-cases/SKILL.md:136-143` journey-deliver has no CommCare→Connect bridge. Code-completable, but exact back-nav gesture needs live post-Learn ui-dump (Learn completion one-way → fresh Phase 6 to validate). | **Draft PR** opened (branch `fix/618-interleg-nav-bridge`): added inter-leg handoff bridge to `connect-resume-opp.yaml` + contract note in `app-test-cases/SKILL.md`. Flagged UNVALIDATED-GESTURE — back-nav depth needs live post-Learn dump before merge. `npm test` green (1853 passed). |
| 650 | investigate | high | M* | ~80 raw resource-ids in static recipes bypass selector-map currency check | Detection half (#652, `bin/ace-doctor:2141-2165`) + claim/resume migration (#663) shipped; ~70 raw ids remain in `connect-login.yaml` (18), `connect-register-from-otp.yaml` (31), `connect-register-to-otp.yaml` (10), `learn-tap-module.yaml` (9), `learn-launch.yaml` (1), `learn-suite-reentry.yaml` (1). Promoting them needs per-id live-device calibration. *M but not code-completable this session — blocked on live device. | **Commented** (blocked-on-live-device) + `question` label. Left open. |
| 666 | investigate | high | — | connect-register-from-otp camera step uses suspect raw ids (wrong package per #593) | 3 raw camera ids still active: `connect-register-from-otp.yaml:256,331,334,337,340,343,346` (`take_photo_button`/`camera_shutter_button`/`save_photo_button`). Live-calibrated `com.android.camera2` entries exist at `connect-2.63.0.yaml:247-255` but recipe doesn't use them. Fix needs a destructive fresh-registration ui-dump on 2.63.0. | **Commented** (blocked-on-live-device) + `question` label. Left open. |
| 629 | investigate | high | — | Phase 6 btn_start inert / start_learn_app handoff never fires | ACE-side hardening shipped (#645): `connect-claim-opp.yaml:462-503` fail-loud wedge diagnostics + `connect_preflight_learn_app_user` atom (`mcp/connect-server.ts:1004-1041`, `mcp/connect/backends/commcare-preflight.ts:214-236`). Substantive fix is the Connect-platform handoff (external system ACE doesn't own); empty `learn_app.name` is Connect-resolved, not an ACE input. Needs live repro to capture the `start_learn_app` HTTP response. | **Commented** (Connect-platform + live-repro) + `question` label. Left open. |

## Summary
- **close:** 1 (#660 — docs shipped)
- **implement:** 1 (#618 — recipe/skill nav-bridge; live-validation residual)
- **investigate:** 3 (#650, #666, #629 — all blocked on a live device or an external/Connect-platform surface)

Notable: the three `investigate` issues are not stalled triage — they're correctly-open issues whose remaining work is gated on live-device calibration (#650, #666) or a downstream Connect-platform fix (#629), which code-only triage cannot complete.
