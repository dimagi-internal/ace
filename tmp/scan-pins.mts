import { scanApkPinSites, mustFlipSites, filesRequiringChecklistMention } from '/Users/acedimagi/emdash/worktrees/ace-apk-2640/lib/apk-pin-sites.ts';
const scan = scanApkPinSites(process.cwd());
console.log('=== SITES ===');
for (const s of scan.sites) console.log(`${s.kind.padEnd(22)} ${s.version.padEnd(8)} ${s.path}:${s.line}  [${s.form}]`);
console.log('\n=== SUSPECTS ===');
for (const s of scan.suspects) console.log(`${s.version} ${s.path}:${s.line} ${s.snippet}`);
console.log('\n=== MUST FLIP ===');
for (const s of mustFlipSites(scan)) console.log(`${s.path}:${s.line} (${s.kind}) ${s.version}`);
console.log('\n=== FILES REQUIRING CHECKLIST MENTION ===');
console.log(filesRequiringChecklistMention(scan).join('\n'));
